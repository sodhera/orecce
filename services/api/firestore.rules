rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // v0 prototype: open access for fast iteration.
    // TODO(auth): lock this down before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

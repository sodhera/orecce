# Web Scope Agent Notes

Scope: `web/`

## Rules
- Touch only files in `web/` unless explicitly instructed.
- Keep this app lightweight and focused on backend integration testing.
- Do not couple to mobile app internals.

## Validation
- Run `npm --prefix web run build` after meaningful changes.
